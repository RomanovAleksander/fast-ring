*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
# Release signing material must never be committed (CLAUDE.md).
keystore.properties
*.jks
*.keystore
# ...except the debug key, which has to be identical on every machine or each
# build gets a different signature and updates stop installing over the top.
!app/debug.keystore
